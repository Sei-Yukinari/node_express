machine:
  environment:
    NODE_ENV: development
  node:
    version: "6.9.1"
  post:
    - "npm install -g npm@3"
  timezone: Asia/Tokyo
  test:
    override:
      - "exit 1"
  deployment:
    production:
      branch: master
      commands:
        - https://registry.hub.docker.com/u/seiyukinari/node_express/trigger/$DOCKER_HUB_TOKEN/